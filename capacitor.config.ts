import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clapsApp.app',
  appName: 'claps-app',
  webDir: 'dist/claps-app',
  bundledWebRuntime: false
};

export default config;
